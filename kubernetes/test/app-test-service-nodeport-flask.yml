---
kind: Service
apiVersion: apps/v1
metadata:
    name: flask-app-nodeport-service
spec:
    type: NodePort
    selector:
        app: flask-app
    ports:
        - port: 5000
          targetPort: 5000